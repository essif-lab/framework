image: node:13.7

stages:
  - build
  - deploy

before_script:
  - node -v

cache:
  untracked: true
  paths:
    - node_modules/

before_script:
  - yarn

build:
  stage: build
  script:
    - yarn parse
    - yarn glossary
    - STAGING=true yarn build

pages:
  stage: deploy
  script:
    - yarn parse
    - yarn glossary
    - STAGING=true  yarn build
    - rm -rf public
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
