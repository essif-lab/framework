image: node:13.7

before_script:
  - node -v

cache:
  untracked: true
  paths:
    - node_modules/

before_script:
  - yarn

pages:
  script:
    - STAGING=true  yarn build
    - rm -rf public
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
